export const baseUrl = 'http://localhost:8000/api/'

export const pizzasBaseUrl = `${baseUrl}pizzas/`

export const addPizzaUrl = `${baseUrl}addPizza/`

export const removePizzaUrl = `${baseUrl}removePizza/`

export const usersBaseUrl = `${baseUrl}users/`

export const registrationUrl = `${baseUrl}registration/`

export const loginUrl = `${baseUrl}login/`

export const logoutUrl = `${baseUrl}logout/`

export const refreshUrl = `${baseUrl}refresh/`
